import React from "react";
import Task from "./Task";

function TaskList({tasks}) {

//  const myTask = tasks.map((tasks) => (
//       <Task
//         category = {tasks.category}
//         text = {tasks.text}
//         // key = {idx}
//         />
//  ))

  return (
    <div className="tasks">
      {/* {myTask} */}
    </div>
  )
  }
export default TaskList;
